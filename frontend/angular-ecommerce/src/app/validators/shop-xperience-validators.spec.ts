import { ShopXperienceValidators } from './shop-xperience-validators';

describe('ShopXperienceValidators', () => {
  it('should create an instance', () => {
    expect(new ShopXperienceValidators()).toBeTruthy();
  });
});
